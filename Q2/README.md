## Key Learning
- Learned two approaches: sorting and hashmap
- Understood frequency counting using dictionary
- Realized hashmap improves time complexity from O(n log n) to O(n)
- Learned to compare character frequencies between two strings
- Practiced handling edge cases like unequal lengths
